import {MdLogin} from "react-icons/md";
import type {IconsType} from "../icons_type.ts";

const LoginIcon = ({classes}:IconsType) => {
    return (
        <MdLogin className={classes}/>
    )
}

export default LoginIcon