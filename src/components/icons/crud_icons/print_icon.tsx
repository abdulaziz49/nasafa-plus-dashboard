import {MdPrint} from "react-icons/md";
import type {IconsType} from "../icons_type.ts";

const PrintIcon = ({classes}: IconsType) => {
    return (
        <MdPrint className={classes}/>
    )
}

export default PrintIcon