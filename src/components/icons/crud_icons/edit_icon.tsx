import {MdEdit} from "react-icons/md";
import type {IconsType} from "../icons_type.ts";

const EditIcon = ({classes}: IconsType) => {
    return (
        <MdEdit className={classes}/>
    )
}

export default EditIcon