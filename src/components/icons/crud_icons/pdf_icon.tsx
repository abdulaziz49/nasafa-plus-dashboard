import {MdPictureAsPdf} from "react-icons/md";
import type {IconsType} from "../icons_type.ts";

const PDFIcon = ({classes}: IconsType) => {
    return (
        <MdPictureAsPdf className={classes}/>
    )
}

export default PDFIcon