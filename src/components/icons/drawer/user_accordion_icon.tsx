import {FaHouseUser} from "react-icons/fa6";
import type {IconsType} from "../icons_type.ts";

const UserAccordionIcon = ({classes}: IconsType) => {
    return < FaHouseUser className={`${classes}`}/>
}

export default UserAccordionIcon