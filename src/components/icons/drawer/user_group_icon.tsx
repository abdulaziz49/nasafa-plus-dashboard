import { FaUserGroup } from "react-icons/fa6";
import type {IconsType} from "../icons_type.ts";

const UserGroupIcon = ({classes}: IconsType) => {
    return < FaUserGroup className={`${classes}`}/>
}

export default UserGroupIcon